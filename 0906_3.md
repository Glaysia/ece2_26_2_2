# 2026-09-06 OT 초안: 분기와 반복은 어떻게 전기 회로가 되는가

## 1. OT의 중심 문장

> 컴퓨터는 값을 기억하고 계산하며, 조건에 따라 다른 일을 하고, 필요한 일을 반복합니다. 이 수업에서는 그 분기와 반복이 실제 전기 회로에서 어떻게 동작하는지 이해하고 직접 구현합니다.

‘전기가 흐르는 거의 모든 장치의 작동 원리를 이해한다’는 큰 목표를 출발점으로 삼는다. 한 학기에 모든 소자의 물리 원리까지 배우겠다는 뜻보다는, 낯선 장치도 입력·처리·출력·전원으로 나누고 어떤 지식이 더 필요한지 찾아갈 수 있는 기초를 만든다는 뜻으로 설명한다.

## 2. 시작: 컴퓨터는 어떻게 분기하고 반복하는가?

첫 화면에는 `if`와 `while`이 들어 있는 짧은 C 코드를 보여 준다. 학생이 배웠던 프로그램에서 출발하여, 그것을 실행하는 회로로 질문을 옮긴다.

```c
int count = 0;
while (count < 10) {
    if (count < 5) {
        // 동작 A
    } else {
        // 동작 B
    }
    count = count + 1;
}
```

발표 멘트 초안:

> 여러분은 C를 배우면서 조건문과 반복문을 써 봤을 겁니다. 문법을 잊었더라도 조건에 따라 다른 일을 하고, 같은 일을 여러 번 반복한다는 뜻은 기억할 겁니다.
>
> 컴퓨터가 복잡한 일을 하는 방식도 여기서 출발합니다. 값을 읽고 계산하고, 조건에 따라 다음에 할 일을 고르고, 필요한 일을 반복합니다. 그 과정에서 중간 결과와 현재 상태를 기억합니다.
>
> 그런데 전기가 흐르는 회로가 어떻게 조건을 판단할까요? 값은 어디에 기억하고, 어떻게 이전으로 돌아가 같은 일을 반복할까요? 지금까지는 컴퓨터가 이미 있는 상태에서 코드를 작성했습니다. 이번에는 그 동작을 가능하게 하는 하드웨어와 실제 입출력을 연결해 봅니다.

학생에게 먼저 물을 질문:

- `count`는 어디에 기억되어 있을까?
- 5보다 작은지는 무엇이 판단하고, A와 B 중 하나는 어떻게 선택할까?
- 다음 반복에서 값은 어떻게 바뀌고, 10이 되면 어떻게 멈출까?

정답을 아는지 시험하는 시간이 아니라, 한 학기 동안 답할 질문을 보여 주는 시간으로 사용한다.

### 코드의 동작을 회로의 말로 바꾸기

| 생각하는 동작 | 필요한 회로 기능 | 수업 내용 |
|---|---|---|
| 값을 기억한다 | 저장과 갱신 | 플립플롭, 레지스터 |
| 계산하고 조건을 판단한다 | 연산과 비교 | 가산기, 비교기, 조합논리 |
| 조건에 따라 다른 일을 한다 | 결과 또는 다음 상태의 선택 | MUX, 상태 전이 논리 |
| 진행 상황을 기억하며 반복한다 | 상태를 되먹임하고 갱신 | 카운터, 순차논리 |
| 순서를 바꾸거나 멈춘다 | 조건에 따른 상태 갱신과 유지 | FSM, enable |

분기와 반복을 중심으로 설명하되 연산과 기억도 함께 필요하다는 점을 연결한다. 위 표는 코드 한 줄이 특정 부품 하나로 번역된다는 뜻이 아니다. 클록은 값을 갱신할 시점을 정하며, 무엇을 반복하고 언제 끝낼지는 설계한 논리가 결정한다.

## 3. 전자전기컴퓨터공학부의 지식을 연결하는 수업

장치를 다음과 같이 나누어 본다.

| 부분 | 하는 일 | 예 | 연결되는 지식 |
|---|---|---|---|
| 입력 | 외부 상태를 전기 신호로 받는다 | 버튼, 온도 센서 | 회로, 센서, 전자회로 |
| 처리·기억 | 조건에 따라 분기하고 상태를 갱신하며 반복한다 | 설정값, 타이머, 운전 상태 | 디지털논리, 컴퓨터 구조, 프로그래밍 |
| 출력·구동 | 결정한 동작을 실제 세계에 전달한다 | 모터 구동, 표시등 | 전자회로, 전력전자, 전기기기 |
| 전원 | 각 부분에 필요한 전력을 공급한다 | 제어 회로와 구동부의 전원 | 회로, 전력 변환 |

> 전기, 전자, 컴퓨터가 실제 제품 안에서는 따로 떨어져 있지 않습니다. 프로그램이 계산한 결과는 결국 전기 신호가 되어야 하고, 그 신호는 실제 회로와 연결되어야 합니다. 반대로 실제 세계의 변화가 프로그램에 들어오려면 센서와 회로를 거쳐야 합니다.

이 과목은 특히 가운데의 디지털 제어와 하드웨어·소프트웨어 연결을 직접 다룬다. 모터나 전원 회로의 세부 설계까지 모두 다루지는 않지만, 어디에 그 회로가 필요하고 제어부와 어떻게 연결되는지 이해하도록 한다.

## 4. 처음에는 LED지만, 배우는 것은 장치의 기본 구조

| 수업에서 만드는 것 | 그 안에서 배우는 것 | 다른 장치로 이어지는 예 |
|---|---|---|
| 스위치로 LED 제어 | 입력과 출력, 논리 조건 | 센서 조건에 따른 경고등 |
| 값을 저장하는 레지스터 | 상태와 설정값의 기억 | 횟수·목표값·운전 모드 설정 |
| 카운터와 타이머 | 시간과 횟수 측정 | 자동 종료, 주기적 센서 측정 |
| PWM | 신호의 주기와 high 구간 제어 | LED 밝기, 구동 회로에 보내는 제어 신호 |
| 상태 머신 | 상황에 따라 동작 순서를 바꾸기 | 대기·운전·정지·오류 상태 |
| 보드 사이의 통신 | 명령과 측정값 전달 | 제어부와 별도 기능 모듈의 연결 |

> LED를 켜는 것은 작은 출발점입니다. 여기서 배우는 입력, 기억, 시간, 상태, 통신은 더 큰 장치에서도 다시 등장합니다. 중요한 것은 예제의 크기보다 그 안의 구조를 알아보는 것입니다.

## 5. FPGA는 왜 사용하는가?

### 원하는 디지털 회로를 직접 구성해 보기 위해

> 이미 만들어진 장치를 사용하는 것만으로는 그 내부에서 논리가 어떻게 동작하는지 보기 어렵습니다. FPGA에서는 입력을 어떤 논리로 처리할지, 어떤 값을 저장할지, 여러 회로를 어떻게 연결할지 직접 정합니다.

FPGA에는 설정 가능한 논리 자원, 저장 요소와 연결 자원이 있다. HDL로 기술한 설계를 도구가 이 자원에 배치하고 연결한다. 새 반도체를 제작하지 않고도 디지털 회로의 구성을 바꾸어 실험할 수 있다.

학생에게 전달할 핵심:

- Verilog는 회로의 동작과 연결을 기술하는 언어다.
- 여러 회로가 같은 시간에 동작할 수 있다.
- 클록이 들어올 때 어떤 값이 바뀌는지 직접 설계한다.
- 코드가 그럴듯한지만 보는 것이 아니라, 파형으로 시간에 따른 동작을 확인한다.

예를 들어 카운터 두 개를 만들면 두 회로가 함께 동작할 수 있다. 학생은 ‘코드를 어느 줄부터 실행하나’만 생각하는 대신 ‘어떤 회로들이 연결되어 있고 언제 값이 바뀌나’를 생각하게 된다.

FPGA를 사용하는 교육적 이유는 모든 제품에 FPGA가 들어가서가 아니다. 디지털 장치 내부의 구조를 직접 만들고 관찰할 수 있기 때문이다.

앞의 C 예제와 연결하면, 카운트 값을 기억할 레지스터, 값을 증가시키는 연산 회로, 조건을 확인할 비교기, 결과와 다음 값을 선택하는 논리를 직접 구성하는 경험이다.

### 같은 문법이 같은 실행 방식을 뜻하지는 않는다

- 합성되는 Verilog의 `if`는 문맥에 따라 결과 선택이나 레지스터 갱신 조건 등을 표현한다. CPU의 분기 명령어를 실행한다는 뜻은 아니다.
- 합성 가능한 고정 횟수 `for`는 여러 연산이나 연결을 회로로 펼쳐 표현할 수 있다. 반복 횟수만큼 클록이 흐른다는 뜻은 아니다.
- 여러 클록에 걸쳐 반복하려면 카운터와 상태, 레지스터의 갱신을 설계한다.
- 테스트벤치의 반복문은 시뮬레이터가 실행하는 검증 코드이므로 회로를 합성하는 코드와 구분한다.

OT에서는 차이가 있다는 점만 예고하고 실습에서 다시 설명한다.

## 6. MCU는 왜 사용하는가?

### 이미 만들어진 회로를 프로그램으로 활용해 보기 위해

> 실제 제품을 만들 때는 모든 회로를 처음부터 설계하지 않습니다. 필요한 기능이 들어 있는 칩을 고르고, 그 기능을 설정하고, 프로그램으로 연결해서 원하는 동작을 만듭니다. MCU에서는 이 방법을 경험합니다.

MCU는 CPU, 메모리, GPIO, 타이머, 통신 같은 기능을 한 칩에 모은 마이크로컨트롤러다. 이번 수업에서는 STM32를 사용한다.

학생은 C 프로그램으로 주변장치를 설정하고, 입력을 읽고, 동작을 결정한다. 타이머나 통신장치는 설정 후 하드웨어에서 동작할 수 있으므로, CPU가 모든 신호를 일일이 만들어야 하는 것은 아니다.

C 프로그램을 컴파일하면 CPU가 실행할 명령어가 만들어진다. CPU는 값을 읽고 연산하며 조건에 따라 다음에 실행할 명령어의 위치를 바꿀 수 있다. 반복도 이러한 실행 흐름으로 구현할 수 있다. MCU에서는 이미 만들어진 컴퓨터에 프로그램을 넣어 분기와 반복을 수행하고, 이를 실제 입출력과 연결한다.

### 용어 메모: MCU와 ASIC

OT에서는 `MCU(ASIC)`처럼 두 용어를 동의어로 쓰지 않는다. MCU는 칩의 기능·구성에 대한 이름이고, ASIC은 특정 용도를 위해 설계한 집적회로를 가리킨다. 범용으로 판매되는 STM32 MCU를 설명할 때는 ‘이미 설계·제작된 CPU와 주변장치 회로를 사용하는 칩’이라고 표현하면 의도가 분명하다.

‘직접 회로를 구성하는 경험’과 ‘완성된 칩을 설정하고 활용하는 경험’을 대비한다. ASIC 자체의 설계·제작 실습을 하는 것으로 설명하지 않는다.

## 7. 두 가지 방법을 함께 배우는 이유

| 질문 | FPGA에서 해 보는 일 | MCU에서 해 보는 일 |
|---|---|---|
| 일정한 주기의 신호를 만들려면? | 카운터와 비교 논리를 구성한다 | 내장 타이머를 설정한다 |
| 값을 기억하려면? | 필요한 레지스터와 갱신 조건을 설계한다 | 변수·메모리와 주변장치 레지스터를 사용한다 |
| 동작을 바꾸려면? | 논리와 상태 전이, 연결을 바꾼다 | 프로그램과 주변장치 설정을 바꾼다 |
| 제대로 동작했는지 알려면? | 테스트벤치·파형·보드 계측으로 확인한다 | 디버거·상태값·보드 계측으로 확인한다 |

> 같은 기능도 여러 방법으로 만들 수 있습니다. 이 수업에서는 어느 쪽이 무조건 더 좋은지를 정하는 대신, 무엇을 직접 회로로 만들고 무엇을 프로그램으로 처리할지 생각해 봅니다.

핵심 질문은 ‘분기와 반복을 위한 회로를 직접 구성할 것인가, 명령어를 실행하는 회로를 이용할 것인가, 두 가지를 어떻게 연결할 것인가’이다. CPU 자체를 전부 설계하는 수업은 아니지만 CPU도 논리·저장·제어 회로로 이루어진다는 이해로 이어진다.

통합 프로젝트의 간단한 예:

- STM32는 사용자 명령을 받아 밝기나 동작 모드를 정한다.
- 통신으로 설정값을 FPGA에 전달한다.
- FPGA는 설정값에 따라 PWM 신호를 만든다.
- 실제 신호를 측정하고 설정한 값과 맞는지 확인한다.

이 정도의 단순 PWM 기능은 MCU만으로도 만들 수 있다. 여기서는 역할 분담과 연결을 배우기 위한 예제로 사용하며, 프로젝트에서는 왜 두 장치를 함께 쓰는지 각 조가 설명하도록 한다.

## 8. AI를 사용하는데도 왜 배워야 하는가?

> AI는 코드 작성을 도와줄 수 있습니다. 하지만 어떤 입력이 들어오고 어떤 출력이 나와야 하는지, 실제 보드가 어떻게 연결되어 있는지, 측정 결과가 요구사항과 맞는지는 우리가 확인해야 합니다.
>
> 이 수업에서는 AI를 사용해도 됩니다. 대신 AI가 만들어 준 결과를 실행하고, 바꾸고, 검증할 수 있어야 합니다. 값 하나를 바꾸었을 때 어떤 일이 일어날지 예상하고, 실제 결과로 확인하는 것이 중요합니다.

학생이 먼저 정할 것은 초기 상태, 분기 조건, 반복할 동작과 종료 조건이다. 입력이 중간에 바뀌거나 다시 시작하면 어떻게 되는지도 확인한다. AI가 코드를 써 주더라도 이 동작의 정의와 검증이 필요하다.

기본 진행 방식:

1. 원하는 동작을 짧고 분명하게 적는다.
2. 먼저 결과를 예상한다.
3. 예제와 AI 도움으로 코드를 작성하거나 수정한다.
4. 시뮬레이션과 실제 보드에서 확인한다.
5. 예상과 다르면 원인을 찾고 다시 검증한다.

처음부터 많은 문법을 알고 있어야 시작할 수 있는 수업으로 소개하지 않는다. 필요한 기초부터 배우되, 실행 결과를 스스로 확인하는 책임은 학생에게 있다는 점을 전달한다.

## 9. 학기 말에 할 수 있기를 기대하는 일

> 처음 보는 장치를 만났을 때 ‘이건 모르겠다’에서 멈추지 않고, 입력은 무엇이고, 무엇을 기억하고, 무엇을 제어하며, 각 부분이 어떻게 연결되어 있는지 질문할 수 있으면 좋겠습니다.
>
> 그리고 작은 기능 하나라도 직접 설계해서 실제로 동작시키고, 왜 동작하는지 설명할 수 있어야 합니다. 우리가 한 학기 동안 연습할 것은 그 과정입니다.

구체적으로는 다음을 목표로 한다.

- 장치의 기능을 입력·처리·출력으로 나누어 블록도를 그린다.
- 간단한 디지털 회로와 MCU 프로그램을 작성·수정한다.
- 두 장치가 주고받을 신호와 데이터를 정한다.
- 파형과 측정값을 근거로 정상 동작 여부를 판단한다.
- 동작하지 않을 때 입력, 코드, 연결, 타이밍 중 어디부터 확인할지 생각한다.

## 10. OT 도입부 발표 순서 제안

약 15~20분 분량의 도입부로 시작하고, 이후 일정·평가·보고서·장비 안내로 넘어간다.

| 화면 | 제목 | 전달할 내용 |
|---|---|---|
| 1 | 컴퓨터는 무엇을 하는 장치인가? | 분기와 반복, 연산과 기억 |
| 2 | if와 while은 실제로 어떻게 동작할까? | 짧은 C 예제에서 회로로 질문 옮기기 |
| 3 | 회로는 어떻게 판단하고 기억할까? | 비교·선택·레지스터·상태 갱신 |
| 4 | 코드가 실제 장치의 동작이 되기까지 | 전기·전자·컴퓨터의 연결 |
| 5 | FPGA: 회로를 직접 구성해 보기 | 디지털 하드웨어의 구조와 동작 |
| 6 | MCU: 만들어진 회로를 활용하기 | 프로그램과 주변장치 제어 |
| 7 | 두 장치를 연결하면 | 역할 분담과 시스템 통합 |
| 8 | AI와 함께 만들고 직접 검증하기 | 예측·실행·측정·설명 |
| 9 | 학기 말에는 | 낯선 장치를 분석하고 작은 기능을 구현하는 능력 |

마지막 연결 멘트:

> 이제 이 목표를 향해 어떤 순서로 실습하고, 무엇을 제출하고, 어떻게 평가할지 설명하겠습니다.

이 문서는 OT의 목적·동기 부여 부분을 위한 초안이다. 일정과 프로젝트 운영은 수업계획서, 보고서 조건은 `0906_2.md`를 바탕으로 별도 구성한다.
