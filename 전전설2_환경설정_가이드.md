# 개요 
## HDL이란
- 필요한 툴 들 설치 방법 및 예비 실습 가이드입니다.
- 학생회관 아느칸 옆에 학기 초에 빈집털이를 하면 노트북을 얻을 수 있음
    - 사양은 10세대 i5에 8GB인데 답답함
    - 이번 학기에는 Verilog HDL(or VHDL or SystemVerilog)을 사용해 반도체 회로를 구성하는 시스템 중 논리회로를 직접 만들어보고 이를 활용한 프로젝트를 해보고 마무리됨
    - HDL은 Hardware Description Language인데 우리가 배운(python, c 등) 일반적인 언어는 
    ```c/cpp
    #include <stdio.h> //표준 입출력 라이브러리
    
    int main(int argc, char* argv){ //표준 입력 파이프라인에 연결됨
    
        int id = 2021440107;
        printf("제 학번은 %d 였습니다 \n", id); //표준 출력 파이프라인에 연결됨
        
        return 0;
    }
    ```
    - 프로그래밍 언어로 위의 코드를 컴파일러라는 프로그램이 먹고 0x0de1934a 0x10433e93 ... 의 기계가 읽을 수 있는 기계어(숫자열)로 변환됨 cpu는 숫자열을 먹고 숫자열을 내뱉음
    - 우리가 이번 학기에 다룰 HDL은 프로그램을 만드는게 아닌 cpu와 같은 논리회로를 내뱉음
    ```verilog
    module toplevel(
      input wire clk,
      input wire rst
    );

    reg flop1, flop2;

    always @(posedge clk or posedge rst) begin
      if (rst) begin
        flop1 <= 1'b0;
        flop2 <= 1'b1;
      end
      else begin
        flop1 <= flop2; 
        // <=와 =가 다름 blocking/nonblocking이 뭔지 나중에 알아보세요
        flop2 <= flop1;
      end
    end
    endmodule
    ```
### 한 줄 요약
- 프로그래밍 언어는 CPU 위에서 돌아가는 프로그램을 만드는데 HDL은 CPU 등의 논리회로를 만듦
## ASIC과 FPGA
- ASIC(Application-Specific Integrated Circuit)은 특정 목적을 가지고 만든 반도체 집적회로를 말함 (ex 휴대폰 APU, 컴퓨터 CPU, GPU, 에어컨, tv, 각종 가전제품에 들어가는 칩셋)
- 반도체 칩셋의 제조 과정에서 웨이퍼에 수십, 수백 칩을 동시에 제조하기 때문에 칩을 생산해서 로직을 확인하는 데에는 비용이 굉장히 많이 듦
- 또한 반도체의 실시간 고성능이 꼭 필요한 분야인데 수요가 크지 않은 경우도 있음
- FPGA(Field-Programmable Gate Array)는 ASIC를 제조하지 않고도 같은 성능과 로직을 검증할 수 있게 연결 가능한 범용 블록들을 넣어둔 반도체로, 양산 전 테스트 또는 특정 분야에 가속기 역할로 사용 되는 경우가 많음(ex FPGA 딥러닝 추론 가속기, 비트코인 채굴기, 물리현상 실시간 시뮬레이터)
## 사용할 툴
- 필수 
    - iverilog(베릴로그 린터)
        - MSYS2(iverilog 실행 위해 필요한 툴), 20MB
    - vscode, 2GB
    - Xilinx Vivado, 100GB 
- 선택
    - git, 10MB
- 사족
    - 윈도우 PC명/유저명 한글이면 유저를 새로 만들어야 함 파워셸에서 whoami 쳤을 때 한글이 한글자라도 있으면 유저 새로 파야 함 바꿔도 안됨 PC명은 그냥 바꾸면 됨 
    - 맥북 유저는 https://github.com/ichi4096/vivado-on-silicon-mac 이게 된다던데 로제타 지원 종료되면 모르겠네 해보고 안되면 학관에서 노트북 빌리기 전전설2용으로 
    - 리눅스 유저 있는지 모르겠는데 리눅스에서는 다 쉽게 잘 됨 알아서 하기
    - 간혹 윈도우 노트북인데 스냅드래곤 ARM64 달려서 아무것도 안되는 노트북이 있는데 다 안됨 노트북 빌리기
    - 항상 뭘 설치하면 창을 껐다 켜야 적용 됨

### 설치 방법
- [MSYS2설치](https://www.msys2.org/)
    - msys2-x86_64-20YYMMDD.exe
    - 버전 상관 없고 그냥 깔면 됨 
    
- iverilog 설치
    - MSYS2 UCRT64 켜서 다음 명령 실행
    ```bash 
    which pacman && pacman -S mingw-w64-x86_64-iverilog    
    ```
    - 환경변수 설정
    ```
    C:\msys64\mingw64\bin를 시스템 환경변수 PATH에 적용하기
    ```
    - **설치 완료된 후 파워셸, vscode에서 다음 명령이 모두 잘 떠야 설치가 잘 된 것임**
    ```powershell
    get-command iverilog.exe
    get-command vvp.exe
    iverilog.exe -h
    vvp.exe -h
    ```

- vscode 설치
    - 학교 네트워크는 상용 툴 설치 사이트가 막혀있는 경우가 많음
    - 집에서 설치해오세요
    - 버전 상관 없음
    - 베릴로그 확장 플러그인 설치하기
        - vscode-slang 
        - vaporview
    - 간단한 소스코드 시뮬레이션 확인하기

- vivado 설치
    - 위의 세 툴만 있으면 베릴로그 코딩과 시뮬레이션은 모두 할 수 있음 그러나 실험실의 FPGA실습킷에 직접 HDL을 합성해서 실험해보기에는 다소 불편하고 어려움 
    - 버전은 2026-1을 사용함
    - [vivado설치](https://www.amd.com/en/support/downloads/adaptive-socs-and-fpgas/development-tools/2026-1.html) 
        - (AMD Unified Installer for FPGAs & Adaptive SoCs 2026.1: Windows Self Extracting Web Installer)
    - 미국의 개발용 전략 소프트웨어는 적성국에서 사용하지 못하게 회원가입과 개인정보 입력이 필요함 AMD 회원가입 하고 다운로드 받기(파일명: FPGAs_AdaptiveSoCs_Unified_SDI_2026.1_0616_1700_Win64.exe)
    - Spartan-7만 설치하면 됨
    - 인터넷 원활한 집에서 설치해도 4시간 걸림 학교에서는 10시간 정도
    - 라이선스 꽤 어렵 잘 보고 따라하기
    - 처음 사용하는 프로그램과 라이선스 세팅은 꽤 까다롭고 어려운 영역인데 끈기를 가지고 누르다 보면 결국 됨 ppt파일에서는 잘 정리해서 따라하면 됨
- 프로젝트 생성
    - 하나의 프로젝트로 한학기 내내 사용할 예정
    - 어우 잘된다 잘 돼 시뮬레이션도 vscode랑 vivado랑 동일한 것 확인
    - 여기까지 다 따라올 수 있으면 한 학기 프로젝트 하는 데에 무리 없음