- 필요한 툴 들 설치 방법 및 예비 실습 가이드입니다.
- 학생회관 아느칸 옆에 학기 초에 빈집털이를 하면 노트북을 얻을 수 있음
	- 사양은 10세대 i5에 8GB인데 답답함
	- 이번 학기에는 Verilog HDL(or VHDL or SystemVerilog)을 사용해 반도체 회로를 구성하는 시스템 중 논리회로를 직접 만들어보고 이를 활용한 프로젝트를 해보고 마무리됨
	- HDL은 Hardware Description Language인데 우리가 배운(python, c 등) 일반적인 언어는 
	```c/cpp
	#include <stdio.h> //표준 입출력 라이브러리
	
	int main(int argc, char* argv){ //표준 입력 파이프라인에 연결됨
	
		int id = 2021440107;
		printf("제 학번은 %d 였습니다 \n", id); //표준 출력 파이프라인에 연결됨
		
		return 0;
	}
	```
	- 프로그래밍 언어로 위의 코드를 컴파일러라는 프로그램이 먹고 0x0de1934k 0x10433e93 ... 의 기계가 읽을 수 있는 기계어(숫자열)로 변환됨 cpu는 숫자열을 먹고 숫자열을 내뱉음
	- 우리가 이번 학기에 다룰 HDL은 프로그램을 만드는게 아닌 cpu와 같은 논리회로를 내뱉음
	```verilog
	module toplevel(
	  input wire clk, rst;
	)
	reg flop1, flop2;
	always @(posedge rst or posedge clk)
	  if (rst)
	    begin
	    flop1 <= 0;
	    flop2 <= 1;
	    end
	  else
	    begin
	    flop1 <= flop2; // <=와 =가 다름 blocking/nonblocking이 뭔지 나중에 알아보세요
	    flop2 <= flop1; 
	    end
	endmodule
	```

