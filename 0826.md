https://chatgpt.com/share/6a8e7200-4b70-83e9-bb68-b863ac6b73d1

이 방향이면 이번 학기에는 **FPGA를 버리는 개편**보다 **“FPGA 단독 실습 → FPGA+MCU hardware/software co-design 실습”으로 과목 성격을 바꾸는 것**이 가장 자연스럽다. 네 수강 이력에서도 디지털논리설계 → 마이크로프로세서 → 전전설Ⅱ 순서로 이어졌던 게 확인되니, 선수지식을 실제 시스템 통합에 써먹게 한다는 명분도 좋다. 

그리고 한백 xFLEX의 현재 공식 자료를 찾아보니 기존 교육내용 자체가 이미 조합회로, 카운터, FSM, PWM, 병렬 인터페이스, **UART**까지 들어가 있고, 하드웨어에는 XC7S75 Spartan-7뿐 아니라 **Tiny MCU Cortex-M4 모듈**과 FPGA GPIO 확장도 들어가 있다. 즉 네 개편안은 기존 과목을 엎는 게 아니라, 제조사 교안 자체가 암시하던 FPGA+MCU 방향을 제대로 현대화하는 것으로 설명할 수도 있다. 

### 내가 이번 학기라면 9주를 이렇게 자른다

| 주     | FPGA                                         | STM32                             | 결과물                       |
| ----- | -------------------------------------------- | --------------------------------- | ------------------------- |
| 1     | VS Code 환경, Verilog 문법 복습, combinational     | -                                 | 시뮬레이션만으로 몇 문제 순식간에        |
| 2     | sequential, FF, counter, FSM                 | -                                 | testbench + waveform      |
| 3     | 실제 XC7S75, XDC, LED/7seg/button, clock/reset | -                                 | 첫 bitstream               |
| 4     | PWM, UART TX/RX                              | -                                 | FPGA ↔ PC UART            |
| 5     | -                                            | G474 GPIO, timer, interrupt, UART | STM32 ↔ PC                |
| 6     | UART + packet/register                       | UART + packet/register            | **FPGA ↔ STM32**          |
| 7     | SPI slave 또는 간단한 memory-mapped peripheral    | SPI master                        | FPGA를 MCU peripheral처럼 사용 |
| 8     | 프로젝트용 IP 조립, timing/debug                    | control/application layer         | 미니 통합과제                   |
| 9     | 프로젝트 설계 리뷰 및 통합 실습                           | 동일                                | 프로젝트 착수 가능한 상태            |
| 10~15 | colspan 개념으로 프로젝트                            |                                   | 6주 프로젝트                   |

특히 **6~7주차가 이 과목의 정체성**이 되면 좋다.

기존에는 학생 입장에서

> Verilog 써서 LED 켜고 → FSM 만들고 → UART 하나 만들고 → “그래서 FPGA를 왜 쓰지?”

로 끝났을 가능성이 큰데, 바꾸고 나면

> STM32가 전체 프로그램을 돌리고, FPGA가 병렬 하드웨어/고속 peripheral 역할을 하며, 둘이 UART/SPI로 실제로 통신한다.

가 된다. 그러면 디지털논리설계와 마이크로프로세서를 왜 배웠는지도 연결된다.

### Vivado는 사실상 backend로 밀어버리는 게 좋다

여기서 네 아이디어가 제일 큰 개선점이다. **Vivado GUI 조작을 수업내용처럼 가르치는 시간을 최대한 없애는 것.**

학생의 일상 workflow를

`VS Code → lint → simulation → waveform 확인 → Vivado synth/implementation/bitstream → FPGA program`

으로 고정하면 된다.

Verilog 쪽은 예를 들어:

* `slang` 또는 Verilator: lint
* `iverilog -g2012`: 빠른 simulation
* VCD
* VaporView/GTKWave: waveform
* Vivado: synthesis / implementation / bitstream / hardware programming만

정도로 두면 된다.

그리고 `.vscode/tasks.json`이나 Python script 하나를 제공해서 **Simulate / Build FPGA / Program FPGA** 정도를 버튼으로 만들어버리면 된다.

중요한 건 **“진도를 3배 빨리”가 툴 조작시간에 대해서는 충분히 가능하지만, 개념 진도를 3배로 잡으면 안 된다는 것**이다. 학생이 FSM과 nonblocking assignment를 이해하는 속도까지 3배가 되는 건 아니다.

대신 과거에

* Vivado 프로젝트 생성
* source 추가
* simulation source 추가
* simulator 조작
* waveform 창 찾기
* XDC 설정
* bitstream 생성

여기서 녹던 2시간을 20분으로 만드는 거다.

그러면 남은 시간에 실제 설계를 훨씬 많이 할 수 있다.

### STM32도 마침 지금은 VS Code로 통일하기 아주 좋다

이 부분은 2026년 기준으로 오히려 환경이 네 계획에 맞게 바뀌었다. ST가 현재 **STM32CubeIDE for Visual Studio Code**를 공식 제공하고 있고, CubeMX가 native CMake 프로젝트를 생성하고 VS Code에서 clangd/CMake/Ninja/ST-LINK 디버깅까지 처리하는 구조다. Eclipse 기반 CubeIDE를 학생들에게 또 하나 가르칠 필요가 없다. ([STMicroelectronics][1])

따라서 하나의 workspace 안에 대략

* `fpga/`
* `mcu/`
* `sim/`
* `docs/`

만 두고 **FPGA와 MCU를 똑같이 VS Code에서 개발**하게 만들 수 있다.

이게 교육적으로도 상당히 깔끔하다.

---

### FPGA ↔ G474 전압 문제

여기서는 **아직 바로 직결한다고 확정하면 안 된다.**

한백 공식 xFLEX 자료에는 FPGA SoM 입력 전원으로 +5 V/+3.3 V를 사용하고, base breadboard에도 +5 V/+3.3 V가 나오며 FPGA GPIO 88-pin extension connector가 있다고 명시돼 있다. 하지만 내가 찾은 공개 카탈로그에는 **그 88개 GPIO의 VCCO가 3.3 V라고 명시돼 있지는 않다.** 

Spartan-7 자체가 bank별 VCCO에 따라 I/O standard가 결정되는 놈이니까,

**기존 실습 XDC 파일에서 expansion GPIO 핀의 `IOSTANDARD`가 `LVCMOS33`인지 확인하는 게 제일 빠르다.**

그렇다면 G474와는 거의 이상적이다.

* STM32 출력: 3.3 V
* FPGA LVCMOS33
* GND 공통
* 신호마다 33~100 Ω 정도 직렬저항 넣어주면 실습실 사고 방지에도 좋음

이면 UART/SPI는 그냥 연결하면 된다.

반대로 해당 bank가 LVCMOS25 같은 거면 level shifter를 쓰거나 다른 bank를 찾아야 한다. **보드에 5 V rail이 있다는 사실 자체는 FPGA GPIO가 5 V라는 뜻은 아니다.**

---

### Codex 때문에 오히려 평가방식은 조금 바꾸는 게 좋다

이건 꽤 중요하다.

앞으로

> “UART receiver를 Verilog로 구현하세요.”

만 던지면 과제의 의미가 거의 사라진다. Codex가 너무 잘 만든다.

그 대신 매 실습에서 세 가지를 보게 하는 게 좋다.

1. **실제 동작**
2. **waveform을 보고 timing을 설명할 수 있는가**
3. **자기가 만든 architecture를 설명할 수 있는가**

예를 들어 랜덤하게 한 명 붙잡고

* `baud_tick`이 왜 이 주기로 나오는지
* FSM이 여기서 왜 IDLE로 돌아가는지
* nonblocking assignment를 blocking으로 바꾸면 어떤 문제가 생기는지
* UART RX metastability를 왜 고려해야 하는지
* SPI에서 CPOL/CPHA가 무엇인지

2분만 물어봐도 AI 복붙과 이해도를 거의 바로 구별할 수 있다.

그래서 **AI 사용을 막는 과목보다 AI 사용을 전제로 더 큰 시스템을 만드는 과목**이 훨씬 낫다. 지금 네가 하려는 게 정확히 그 방향이다.

### 프로젝트도 “FPGA만 가지고 기믹 만들기”에서 벗어나야 한다

10개 예시를 만든다면 나는 전부 **STM32와 FPGA 양쪽을 써야 완성되는 주제**로 만들겠다. 예를 들면:

* FPGA logic analyzer + STM32/PC UI
* 디지털 오실로스코프/파형 측정기
* VGA 또는 간단한 영상 출력 + MCU 제어
* FPGA PWM/encoder motor controller + STM32 supervisory control
* 다채널 frequency counter
* FPGA UART/SPI peripheral hub
* FIR/filter accelerator + STM32
* 간단한 logic synthesizer / function generator
* FPGA 기반 protocol analyzer
* 작은 RISC-V softcore 또는 custom accelerator와 STM32 통신

이러면 6주짜리 프로젝트가 단순히 “Verilog 많이 짜오기”가 아니라 **partitioning → interface definition → implementation → integration → debug**가 된다. 전전설이라는 이름에도 훨씬 잘 맞는다.

그리고 하나 행정적으로는 **17명인데 2인 1조 × 8조면 한 명이 남는다.** `7개 2인조 + 1개 3인조`가 가장 무난하다. 3인조는 프로젝트 난도가 약간 높은 주제를 주면 되고.

지금 상황이면 나는 **1~4주 기존 FPGA 교안의 고속 압축판, 5~9주 FPGA+STM32 신규 교안, 10~15주 통합 프로젝트**로 간다. 이 정도면 담당교수가 보더라도 “과목을 멋대로 STM32 과목으로 바꿨다”가 아니라 **기존 FPGA 실험은 모두 보존하면서 후반부 수준을 대폭 올렸다**고 설명할 수 있어서 정치적으로도 안전하다.

[1]: https://www.st.com/content/st_com/en/campaigns/stm32-vs-code-extension-z11.html?ecmp=tt39101_gl_enews_may2024&utm_source=chatgpt.com "Streamline your development process with the STM32 VS code extension 2.0.0 - STMicroelectronics"
